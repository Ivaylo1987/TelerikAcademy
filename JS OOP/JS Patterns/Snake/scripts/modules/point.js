/// <reference path="../_reference.js" />
define(['canvas'], function (canvas) {
    var Point = (function () {
        var Point = function () {
            this.x = random.x;
            this.y = random.x;
        }

        return Point
    }())

    return Point;
})