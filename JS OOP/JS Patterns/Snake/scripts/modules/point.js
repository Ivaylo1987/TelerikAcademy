/// <reference path="../_reference.js" />
define(['canvas'], function (canvas) {
    var Point = (function () {

        var Point = function (x, y) {
            this.x = x;
            this.y = y;
        }

        return Point
    }())

    return Point;
})